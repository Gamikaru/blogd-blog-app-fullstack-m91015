// import { useEffect, useState } from 'react';

// const useAnimation = (trigger, animationClass) => {
//     const [animate, setAnimate] = useState(false);

//     useEffect(() => {
//         if (trigger) {
//             setAnimate(true);
//         } else {
//             setAnimate(false);
//         }
//     }, [trigger]);

//     return animate ? animationClass : '';
// };

// export default useAnimation;